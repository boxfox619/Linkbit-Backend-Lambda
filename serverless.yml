service:
  name: linkbit-backend

# Add the serverless-webpack plugin
plugins:
  - serverless-webpack
  - serverless-offline

provider:
  name: aws
  runtime: nodejs10.x
  region: ap-northeast-2

functions:
  getAddress:
    handler: handlers.addressHanders.getLinkAddress
    events:
      - http:
          method: get
          path: address
          cors: true
  createAddress:
    handler: handlers.addressHanders.createAddress
    events:
      - http:
          method: post
          path: address
          cors: true
  linkAddress:
    handler: handlers.addressHanders.linkAddress
    events:
      - http:
          method: put
          path: address
          cors: true
  unlinkAddress:
    handler: handlers.addressHanders.unlinkAddress
    events:
      - http:
          method: delete
          path: address
          cors: true
  getCertText:
    handler: handlers.certificationHanders.getCertText
    events:
      - http:
          method: get
          path: cert
          cors: true
 
